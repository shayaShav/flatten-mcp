node_modules/
dist/
*.log
*.tgz
*.mcpb
.DS_Store
.vscode/
.playwright-mcp/
.env

# Local-only AI context — not public
/CLAUDE.md

# Interim / internal / operator docs — never tracked
/.dev/

# Stray scratch images at repo root (real assets live in assets/)
/*.png
