// Single in-code source for the package version, consumed by every server entry
// (stdio in index.ts, Streamable HTTP in http-core.ts). Bumped on release along
// with package.json and the other manifests.
export const VERSION = '2.1.0';
